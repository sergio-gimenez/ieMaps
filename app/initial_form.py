from datetime import date
import datetime

class Initialform:
    startAddress = "La Pau"
    endAddress = "Col·legi Claret"
    dayOfSearch = str(datetime.datetime.now()).split(".")[0]    
    dis = True
    taxi = False
    ev = False
